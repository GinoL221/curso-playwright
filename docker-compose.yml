services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.60.0-jammy
    working_dir: /app
    volumes:
      - ./:/app:Z
      - /app/node_modules
    command: sh -c "npm install && npx playwright test"